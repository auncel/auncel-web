# https://github.com/alibaba/spring-cloud-alibaba/wiki/Nacos-config

spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: '101.37.29.47:6399'
      config:
        file-extension: yaml
        server-addr: '101.37.29.47:6399'
  application:
    name: user-center
dubbo:
  scan:
    base-packages: dev.yidafu.user.center
  protocol:
    name: dubbo
    port: -1
  registry:
    address: spring-cloud://localhost
